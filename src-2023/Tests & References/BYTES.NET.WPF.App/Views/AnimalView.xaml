<UserControl x:Class="BYTES.NET.WPF.App.Views.AnimalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BYTES.NET.WPF.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <DockPanel LastChildFill="True">
            <TextBlock Text="{Binding Path=AnimalName}" FontWeight="Bold" DockPanel.Dock="Left" />
            <TextBlock Text="{Binding Path=AnimalType}" Margin="5,0,0,0" />
        </DockPanel>
    </Grid>
</UserControl>
